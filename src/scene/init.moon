p = "module.core.scene."
{
	MainMenu: require "#{p}main_menu"
	GameSelect: require "#{p}game_select"
	Singleplayer: require "#{p}singleplayer"
	Multiplayer: require "#{p}multiplayer"
	NewGame: require "#{p}new_game"
	LoadGame: require "#{p}load_game"
	JoinGame: require "#{p}join_game"
	Settings: require "#{p}settings"
	Mods: require "#{p}mods"
}
